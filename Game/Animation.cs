namespace Game;

public class Animation(int offsetTop, int offsetLeft, int numFrames)
{
    public int offsetTop = offsetTop;
    public int offsetLeft = offsetLeft;
    public int numFrames = numFrames;
}